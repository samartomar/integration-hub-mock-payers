# Render.com Web Service
# https://render.com/docs/blueprint-spec
services:
  - type: web
    name: healthcare-mock-api
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn app.main:app --host 0.0.0.0 --port $PORT
